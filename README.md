# Redmine/Puma + PostgreSQL10 + Nginx

Сборка сервиса Redmine в Docker-Compose.

Dockerfile Redmine основан на сборке из официальной документации, в качестве App-сервера используется совместно с гемом Puma.
